import { Component } from "react";

class Footer extends Component {
  constructor(props) {
    super(props);
  }
  state = {};
  render() {
    return (
      <footer className="section-footer-admin bg-secondary"></footer>
    );
  }
}

export default Footer;
